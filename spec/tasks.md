# 任務清單：投資撥回試算與比較系統

## 第一階段：設定與基礎建設
- [x] **初始化專案結構**
    - 建立包含 HTML5 樣板的 `index.html`。
    - 加入 React, ReactDOM, Babel, Tailwind CSS, Lucide Icons 和 SheetJS 的 CDN 連結。
    - 建立基本的 React App 元件結構。
- [x] **佈局實作**
    - 建立包含側邊欄（左）與主要內容（右）的響應式容器。
    - 實作手機版側邊欄切換按鈕。
    - [ ] 優化 RWD：確保手機/平板上表格可橫向捲動，操作介面友善。

## 第二階段：核心邏輯與測試
- [ ] **重構核心邏輯 (Dual Scenario)**
    - 修改 `payout.test.js` 加入雙情境 (NAV>=9, NAV<9) 的測試案例。
    - 修改 `index.html` 中的 `calculatePayout` 函數，使其同時回傳兩種情境的計算結果。
    - 驗證新舊邏輯的兼容性與準確性。

## 第三階段：UI 元件與狀態
- [x] **側邊欄輸入**
    - [x] 更新 `目標保費` 下拉選單選項 (新增 30萬、50萬)。
    - 實作 `費用率` 下拉選單。
    - 實作 `進場匯率` 輸入框。
    - 實作 `淨值` (NAV) 輸入框。
    - 移除 `進階設定`，改用固定常數。
- [ ] **儀表板表格更新**
    - [x] 修改表格標題以顯示 `情境 B (8<=NAV<9)`。
    - [x] 移除月撥回差額欄位。
- [x] **投資組合總覽**
    - 計算並顯示 `總保費`。
    - 計算並顯示 `總單位數`。
    - 計算並顯示 `平均單位成本`。
    - [ ] 更新總覽卡片：增加情境 B 的預估年撥回顯示。

## 第四階段：功能與互動
- [ ] **Demo 模式更新**
    - 更新 `loadDemoData` 以載入符合新規格的驗證數據 (31,793.65 單位)。
- [ ] **Excel 匯出更新**
    - 更新 `handleExportExcel` 以包含雙情境的欄位資料。
- [x] **剪貼簿複製**
    - 實作 `handleCopyTable` 以複製摘要文字至剪貼簿。
- [x] **列印功能**
    - 加入觸發 `window.print()` 的「列印」按鈕。
    - 確保 CSS 允許依需求列印完整介面（包含側邊欄）。
- [ ] **資料管理實作**
    - 實作 `localStorage` 存取邏輯。
    - 實作「清除全部」按鈕與確認對話框。

## 第五階段：優化與修飾
- [x] **提示說明 (Tooltips)**
    - 為所有輸入標籤加入邏輯說明的 Tooltips。
- [x] **格式化**
    - 確保所有貨幣數值使用 `Intl.NumberFormat` (TWD/USD)。
    - 加入顯示/隱藏小數位的切換功能。
- [x] **鍵盤導航**
    - 設定輸入框的 `tabIndex` 以確保邏輯順序（保費 -> 費用 -> 匯率 -> 淨值 -> 加入）。
    - 加入 `onKeyDown` 處理器，支援按 "Enter" 鍵送出表單。

## 第六階段：文件與說明 (Footer Documentation)
- [x] **動態公式展示**
    - 使用 LaTeX 或 Markdown 風格呈現公式。
- [ ] **逐步示範更新**
    - 更新 Footer 的逐步示範區塊，展示雙情境的計算過程。