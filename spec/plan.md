# 實作計畫 - 投資撥回試算與比較系統

## 技術棧 (Technical Stack)
- **核心**: React 18 (via CDN), ReactDOM
- **樣式**: Tailwind CSS (via CDN)
- **圖示**: Lucide React (via CDN)
- **工具**: SheetJS (Excel .xlsx 匯出)
- **架構**: 單一檔案 SPA (`index.html`)

## 第一階段：設定與基礎建設 (Setup & Infrastructure)
- [x] 初始化 `index.html`，引入 React, Tailwind, 和 Babel 腳本。
- [x] 設定基本佈局：側邊欄 (控制面板) 與主要內容 (儀表板)。
- [x] 優化響應式容器結構 (RWD)：確保手機版側邊欄收合與表格顯示正常。

## 第二階段：核心邏輯與測試 (Core Logic & Testing)
- [x] **重構核心邏輯**:
    - 修改 `calculatePayout` 函數以支援**雙情境計算** (Dual Scenario Calculation)。
    - 同時計算「情境 A (NAV >= 9, 因子 0.042)」與「情境 B (8 <= NAV < 9, 因子 0.036)」的月撥回與年撥回。
    - 移除舊有的單一情境判斷邏輯。
- [x] **更新單元測試**:
    - 更新 `payout.test.js` 以驗證雙情境回傳結構。
    - 加入新的驗證數據測試案例 (31,793.65 單位)。

## 第三階段：UI 元件與狀態 (UI Components & State)
- [x] **側邊欄輸入**:
    - 目標保費 (下拉選單：30萬、50萬、100萬~1000萬 + 自定義輸入，預設 100萬)。
    - 費用率 (下拉選單)。
    - 匯率設定:
        - 進場匯率 (Cost ExRate) (輸入框，預設 31.5)。 
    - 淨值 (NAV) (輸入框)。
    - 實作「快速新增進場」表單：
        - 目標保費 (下拉/自定義)、費用率、進場匯率、淨值。
    - **全域設定 (Header)**: 現時匯率 (Current ExRate) 移至上方，即時連動。
    - **清除功能**:
        - 實作「清除全部」按鈕。
    - **資料持久化**:
        - 實作 LocalStorage 存取邏輯 (Auto-save & Load)。
    - **Tab Index 優化**: 確保順序 (保費 -> 費用 -> 匯率 -> 淨值 -> 新增)。
- [x] **儀表板表格 (Dashboard Table)**:
    - **結構更新**: 實作「情境對照區」欄位佈局。
    - **顯示內容**:
        - 顯示「月撥回」對照 (NAV>=9 vs 8<=NAV<9)。
        - 顯示「年撥回」對照 (NAV>=9 vs 8<=NAV<9)。
    - 保持即時匯率連動。
- [x] **投資組合總覽**:
    - 呈現加碼後總體數據：總保費、總單位數。
    - 計算平均單位成本 (加權平均)。
    - 列操作: 支援即時編輯與刪除。
- [x] **投資組合總覽卡片 (Portfolio Summary)**:
    - 顯示：總投入本金、加權平均單位成本。
    - 顯示：當前預估總撥回 (月/年) - 需並列顯示情境 A 與情境 B。

## 第四階段：功能與互動 (Features & Interactivity)
- [x] **Demo 模式**: 更新為新的驗證數據 (假設現時匯率 30)。
- [x] **匯出**: 更新 Excel 匯出邏輯以包含雙情境對照資料。
- [x] **剪貼簿**: 實作「複製表格」功能。
- [x] **列印**: 實作列印按鈕，需支援列印完整操作介面 (包含側邊欄)。
- [x] **資料管理**:
    - [x] 實作 LocalStorage 自動儲存/還原。
    - [x] 實作「清除全部」按鈕。

## 第五階段：優化與修飾 (Polish)
- [x] 為所有輸入欄位與表頭加入 Tooltips。
- [x] 確保貨幣格式 (TWD/USD) 與千分位分隔符號。
- [x] 驗證 WCAG AA 合規性 (ARIA 標籤、鍵盤導航)。
- [x] **鍵盤導航優化**: 設定正確的 `tabIndex` 與焦點管理，確保輸入流程 (保費 -> 費用 -> 匯率 -> 淨值 -> 加入) 可全鍵盤操作。

## 第六階段：文件與說明 (Documentation)
- [x] **動態公式展示**:
    - 使用 LaTeX 或 Markdown 風格呈現公式。
- [x] **逐步示範**:
    - 更新逐步示範區塊，展示雙情境的計算過程。

## 執行策略 (Execution Strategy)
1.  更新 `payout.test.js` 驗證雙情境邏輯。
2.  修改 `index.html` 中的 `calculatePayout`。
3.  更新表格 UI 以呈現對照資訊。
4.  更新 Demo 數據與匯出功能。
